pipeline {
  options {
    timeout( time:30, unit:'MINUTES') 
  }
  environment {
   NAME = "ABHIJIT" 
  }
  agent {
    node {
      label 'master' 
    }
  }
  
  stages {
    stage("BUILD") {
      steps {
        sh '''
          echo "Testing var $NAME"
        '''
      }
    }
  }
}
